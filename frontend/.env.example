# Backend API URL — this is the ONLY source of truth for all API calls.
# Development: points to local backend
# Production: points to deployed backend (set in .env.production or Vercel dashboard)
VITE_API_URL=http://localhost:5050/api
